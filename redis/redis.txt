things i know:
- i picked this one to be done immedietly after the memcached, because it would allow me to compare the two in the most wholesome way possible

goals:
- understand the differences between the two different types of services and why redis is considered the standard for the most part
- 


things i researched:
- Redis is an in-memory data structure server, which supports storing strings, hashes, lists, sets, sorted sets and more.
- redis clients use resp (redis serialization protocol) to communicate with the redis servers
- does the whole ping/pong thing and the echo thing, pub/sub, and storing values?



things i learned: